import { View, Text } from 'react-native'
export default function AdminNative() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Admin panel is only available on web.</Text>
    </View>
  )
}
